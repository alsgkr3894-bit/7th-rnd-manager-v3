# QA 리포트 — N-45·N-46·B배치(N-05·07·09·18) 2026-06-13

## 검증 범위

이번 세션에서 구현된 항목 + B배치 기구현 확인(N-01~19) + C배치 기구현 확인(N-20~38).

---

## 최종 검증 결과

```
ESLint:  에러 0 / 경고 기존 no-img-element 12건 (신규 없음)
Tests:   794 passed (140 suites)
Build:   ✅ 57 pages prerender — 에러 0
```

---

## 구현 항목별 검증

### N-45 영양성분 시험성적서 기반 입력 (`NutritionInputPanel.jsx`)

| 항목 | 결과 |
|------|------|
| 크러스트 탭 "성적서" 뱃지 | ✅ `safeRawMap[key]?.certLinked === true`일 때 표시 — 저장된 값만 반영 |
| "시험성적서 기반 입력" 체크박스 | ✅ `form.certLinked` / `setField('certLinked', checked)` |
| 자동 저장 | ✅ `useNutritionBaseEditor.handleSave`의 `...form` spread가 certLinked 포함 — 훅 무변경 |
| 미저장 상태 초기값 | ✅ `form` 초기값 `{}` → `!!form.certLinked = false` → 체크박스 unchecked 정상 |
| 기존 탭 도트 뱃지 공존 | ✅ done(kcal 있음) 도트와 cert(certLinked) 뱃지 동시 표시 가능 |

### N-46 제품원가표 탭 (`app/report/cost/page.jsx`)

| 항목 | 결과 |
|------|------|
| `viewTab` state | ✅ `'report'` 기본값, 탭 스위처 2버튼 |
| `groupPizzaLR` 함수 | ✅ 정규식 `/^(.+)\s+(L|R)$/` — L/R 없는 메뉴는 `'단일'` 키로 처리 |
| `activeCats` 구조 | ✅ `[id, catObject]` 튜플 — `.filter(([id]) => ...)` 구조 정상 |
| 피자/1인피자 L/R 7컬럼 테이블 | ✅ 메뉴명·L판매가·L원가·L원가율·R판매가·R원가·R원가율 |
| 사이드/세트/엣지 4컬럼 평면 테이블 | ✅ 메뉴명·판매가·원가·원가율 |
| activeCats 필터 반영 | ✅ 체크 해제 카테고리는 costTable에서도 미표시 |
| paper-foot 제목 분기 | ✅ `viewTab === 'costTable'` ? '제품원가표' : '원가계산 보고서' |
| 위험 원가율 색상 | ✅ `rate >= riskThreshold` → `var(--warn)` |

### N-05 노트작성 제목 required 제거 (`_NoteFormBody.jsx`)

| 항목 | 결과 |
|------|------|
| 제목 Field `required` 제거 | ✅ line 108 — 레이블 * 표시 제거 |
| 에러 스타일링 유지 | ✅ `error={touched.title && !form.title.trim()}` — 빈 제목 입력 후 blur 시 경계선 표시 (UX 유지) |
| "핵심 테스트 내용" required 유지 | ✅ line 204 — 별도 필드, 변경 없음 (의도적) |
| CATEGORIES 토핑 | ✅ `lib/note/constants.js` 이미 `['피자', '사이드', '소스', '도우(엣지)', '기타']` — 토핑 없음 |

### N-07 CSV → 엑셀로 내보내기 명칭 변경

| 파일 | 변경 내용 | 결과 |
|------|-----------|------|
| `lib/download.js:165,173` | 기본 파일명 `'CSV내보내기'` → `'엑셀내보내기'` | ✅ |
| `app/ingredient/list/page.jsx` | "현재 목록 CSV" → "엑셀로 내보내기" | ✅ |
| `app/menu-master/page.jsx` | 토스트 "CSV ${n}개" → "엑셀 ${n}개" | ✅ |
| `app/settings/backup/page.jsx` | 히스토리 "CSV" → "엑셀로 내보내기" | ✅ (버그수정) |
| `app/note/sample/[id]/page.jsx` | "CSV" → "엑셀로 내보내기" | ✅ (버그수정) |
| 기확인 완료 | sample/page·usage·allergen·all-summary·recipe·origin·note board | ✅ 이미 "엑셀로 내보내기" |
| 업로드 설명 "CSV" | jette/shipment·price-compare·menu-sales/upload — **파일 형식 안내 문자열** | ✅ 변경 불필요 |

**버그 수정 커밋**: `87d6248` — backup·sample/[id] 2파일 누락 발견 및 수정

### N-09 다크모드 토글 트랜지션 (`tokens.css`)

| 항목 | 결과 |
|------|------|
| 타겟 요소 한정 | ✅ `body, .topbar, .sidebar, .card, .stat-card, .nav-item, .btn, .chip, .paper-section, .icon-btn` |
| 전역 `*` transition 미사용 | ✅ 성능·애니메이션 충돌 방지 |
| FOUC 방지 스크립트 공존 | ✅ `<head>` 내 인라인 스크립트가 렌더 전 `data-theme=dark` 설정 → 초기 로드 시 전환 깜빡임 없음 |
| transition 속성 | `background-color .18s ease, color .18s ease, border-color .18s ease` |

### N-18 dropzone 박스 축소 (`features.css`)

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| `.dropzone` padding | `28px 32px` | `16px 24px` |
| `.dropzone` gap | `8px` | `6px` |
| `.dropzone-ico` 크기 | `64×64px` | `44×44px` |
| `.dropzone-ico` border-radius | `20px` | `14px` |
| `.dropzone-ico` margin-bottom | `8px` | `4px` |
| `.dropzone-title` font-size | `18px` | `14px` |
| 적용 범위 | jette/price-compare, jette/shipment, menu-sales/upload 등 `.dropzone` 전체 | ✅ |

---

## 기구현 확인 항목 (이번 세션에서 skip)

### B배치 — 이미 구현 확인

| ID | 내용 | 확인 |
|----|------|------|
| N-01 | 노트 탭 순서 | ✅ `lib/menu.js` 일정달력→노트작성→노트목록→칸반보드→연구일지→샘플기록 |
| N-02 | 노트목록 CSV 삭제 | ✅ `_NoteContent.jsx` exportCsv 없음 |
| N-03 | 칸반 CSV/인쇄 삭제 | ✅ `board/page.jsx` CSV/print 없음 |
| N-04 | 달력 보고용복사 삭제 | ✅ `calendar/page.jsx` copyMonthSummary 없음 |
| N-06 | 원가보고서 집계기간 삭제 + 제목 변경 | ✅ "7번가피자 제품원가표 (단가 기준)" + 집계기간 OptGroup 없음 |
| N-08 | 상단바 정렬 | ✅ `.search { flex: 1 }` + 우측그룹 `marginLeft: auto` |
| N-10 | 메뉴마스터 CSV 규격 추가 | ✅ `headers = ['메뉴코드', '메뉴명', '규격', ...]` |
| N-11 | 피자 기본가 일괄 버튼 삭제 | ✅ `handleBulkPizza` 없음 |
| N-12 | 양식업로드 카드 하단 이동 | ✅ `MenuPriceUploadCard` 테이블 하단에 위치 |
| N-13 | 재료단가표 초기화 2단계 | ✅ `btn sm` "초기화" → 확인 후 "진행하기" |
| N-14 | 재료단가표 일괄 업로드 삭제 | ✅ ingredient-price BulkPriceModal 없음 |
| N-15 | 제품별 사용현황 탭 삭제 | ✅ `VIEW_TABS = [price, issues]` |
| N-16 | 정렬 컬럼헤더만 | ✅ `SortButton` 없음, `SortableHeader`만 잔존 |
| N-17 | 사용현황 액션 버튼 상단 | ✅ `/* 액션 버튼 (상단) */` comment 확인 |
| N-19 | 샘플기록 placeholder | ✅ "예) ○○ 0차 샘플" / "식자재명" |

### C배치 — 이미 구현 확인

| ID | 내용 | 확인 |
|----|------|------|
| N-20 | 노트 상태 enum 확장 | ✅ `STATUSES` = 아이디어·샘플테스트·메뉴테스트·보고예정·재테스트·출시예정·보류·출시·폐기 + 테스트중(레거시) |
| N-21 | 연구일지 PDF 공백 | ✅ `journal-print.js` break-inside 조정 완료 |
| N-22 | 샘플 카테고리 분리 | ✅ `SAMPLE_CATEGORIES` from `@/lib/sample` |
| N-23 | 달력 자동일지 맨밑·기본 접힘 | ✅ `_DayPanel.jsx` `logsOpen` 상태, 하단 배치 |
| N-24 | 달력 CSV→PDF | ✅ `exportMonthPdf` 존재, CSV 없음 |
| N-25 | 달력 가독성 | ✅ `CalendarGrid.jsx` 구현 완료 |
| N-26 | 원가보고서 엣지 포함 옵션 | ✅ "피자 원가에 기본 엣지 포함" 체크박스 |
| N-27 | 원가레시피 버튼 sticky | ✅ `RecipeEditor.jsx` `position: sticky` |
| N-28 | 묶음관리 글씨 색상 | ✅ `on ? 'var(--accent)' : 'var(--text-1)'` |
| N-29 | 식자재 량 입력 확대 | ✅ `SortableIngredientRow` padding·width 적용 |
| N-30 | 식자재리스트 PDF 2단 | ✅ `lib/ingredient/print.js` 2컬럼 그리드 + baseQuantity·temperature·priceWithTax |
| N-31 | 재료단가표 이슈탭 | ✅ `VIEW_TABS = [price, issues]`, 시드 버튼 없음 |
| N-32 | 판매량 지난달/이번달 삭제 | ✅ `SalesReportControls` 빠른버튼 없음 |
| N-33 | 가격보고서 변동률 무조건 | ✅ 임계값 슬라이더 없음 |
| N-34 | 가격보고서 전체식자재 요약 | ✅ 통합 요약 테이블 존재 |
| N-35 | 원가보고서 집계기준 삭제 | ✅ 집계기간 버튼 없음 |
| N-36 | 제때 신규/삭제 요약카드 | ✅ `PriceSummaryCards` NEW·DELETED 카드 구현 |
| N-37 | 1인피자 태그 중복 | ✅ `CategoryTags` 조건부 렌더로 중복 방지 |
| N-38 | 알레르기 빈도 정렬 | ✅ `allergen/page.jsx` 빈도 내림차순 기본값 |

---

## 발견된 버그 및 수정

### Bug 1 — N-07 CSV 버튼 텍스트 누락 2건

| 항목 | 내용 |
|------|------|
| 위치 | `app/settings/backup/page.jsx`, `app/note/sample/[id]/page.jsx` |
| 문제 | 버튼 텍스트가 "CSV"로 남아있음 — N-07 명칭 변경 누락 |
| 수정 | "엑셀로 내보내기"로 변경 |
| 커밋 | `87d6248` |

---

## 보류 항목

| ID | 내용 | 대기 조건 |
|----|------|-----------|
| N-42 | 엣지별 알레르기 탭 | 크러스트 변형 기대값 설계 합의 |
| N-43 | 재료단가표 과거 단가 가져오기 | 동작 명세 (조회만? 일시 적용?) |
