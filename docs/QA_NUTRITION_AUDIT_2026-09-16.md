# 영양성분 모듈 정밀 감사 — 2026-09-16

법적 표시(영양성분·알레르기·원산지)를 만드는 모듈이라 **값의 정확성**과 **타 모듈(메뉴마스터·식자재·레시피)
연동**을 코드 검토 + 실서버 데이터 전수 재계산 + 브라우저 실동작 세 겹으로 확인했다.

## 범례
| 표시 | 의미 |
|---|---|
| ✅ | 정상 확인 |
| ⚠️ | 동작하나 주의/데이터 보완 필요 |
| ❌ | 버그(수정함) |
| 🔴 | **데이터 누락 — 법적 표시에 영향, 사용자 입력 필요** |
| ⏭ | 미실행(사유 기재) |

## §0. 중요 — 1차 감사는 낡은 서버 사본 위에서 진행됐음 (같은 날 정정)
1차 감사(서버 67,841행) 후 사용자가 "식자재 다 등록돼 있다"고 해 사용자 PC 백업(`7번가시스템백업_20260916.json`)과
대조한 결과, **운영 PC 브라우저의 저장이 서버로 전혀 동기화되지 않아** 서버 사본이 수개월 낡아 있었다:
영양 메뉴 8/35, 영양 원값 19/67, 레시피 38/78, 식자재 121/151, 도우/엣지 구성 0/5, 출고량 64k/236k. 서버에는
QA 잔재(ZZ* 검증용 식자재 5건이 실제 식자재 id를 점유, 분기행 6건)도 남아 있었다.
→ pg_dump 스냅샷(`.qa-tmp/workflow-db-safety-before-import-*.dump`) 후 `npm run db:import:backup -- <백업> --brand main --include-shared`로
서버를 백업과 일치시킴(47 store 240,765행, 스크립트의 트랜잭션 시간제한 5초 → 30분으로 수정 `965243b2`).
서버에서만 사라진 것: 2026-06 판매량 파일 1건(222행, 사용자 PC에 없던 것)·테스트 토핑 1건·플랫폼 수수료 설정 1건(사용자 승인).
**아래 §1·§2·§4 데이터 항목은 정정 후 실데이터 기준으로 다시 쓴 결과다.** 코드 버그(§4 B1~B6)는 데이터와 무관하게 유효.
**미해결: 운영 PC 브라우저가 왜 서버로 동기화하지 않는지** — `/settings/sync`의 모드(운영 PC/읽기 전용)와 서버·이 PC 행 수 확인 필요.

## 환경
- 서버: 로컬 PostgreSQL `rnd_manager`, Next.js dev `http://localhost:3000`, 브라우저: 프리뷰 탭(운영 PC 모드, admin)
- 데이터(정정 후): 사용자 백업 임포트 → 서버 240,758행(네트워크 비공개 2 store 제외). 영양 스토어: `nutrition_menu_ref` **35**
  (피자 17·1인용 4·사이드 13·음료 2), `nutrition_raw_values` 67, `nutrition_edge_master` 4, `nutrition_topping_master` 13,
  `nutrition_set_composition` 2, `cost_edge_dough` **5**(치즈크러스트 L/R·골드스윗 L/R·씬도우), 식자재 151(알레르기 입력 다수)
- 재계산 스크립트: 스크래치 jest(`@/lib/db`를 서버 API 스냅샷 위 메모리 구현으로 대체, 쓰기 금지) →
  앱 빌더(`buildPizzaSheet`/`buildPizzaSliceSheet`/`buildSideSheet`/`buildToppingSheet`/`buildSetHalfSheet`/`buildBeverageSheet`)
  실행 후 독립 산술(100g값×150/100, 100g값×총중량×조각비율/100, 베이스+엣지 합산, 세트 min/max)과 셀 단위 대조
- 법적 기준(식약처): 조리·판매업소 어린이 기호식품 영양표시 의무 5종 = **열량·당류·단백질·포화지방·나트륨**, 알레르기 유발물질 22종.
  반올림은 "계산값 그대로 또는 5kcal/5·10mg 단위" 모두 허용 → 앱의 정수 반올림은 적법.
  출처: [식약처 보도자료](https://www.mfds.go.kr/brd/99/down.do?brd_id=ntc0021&seq=45555&data_tp=A&file_seq=2),
  [찾기쉬운 생활법령 — 어린이 기호식품 표시](https://easylaw.go.kr/CSP/CnpClsMain.laf?popMenu=ov&csmSeq=1113&ccfNo=2&cciNo=2&cnpClsNo=1),
  [식약처 알레르기 유발 식품 표시](https://www.foodsafetykorea.go.kr/portal/board/boardDetail.do?menu_no=3120&menu_grp=MENU_NEW01&bbs_no=bbs001&ntctxt_no=1091412)

---

## §1. 실데이터 전수 재계산 대조 (코드 수정 + 서버 정정 후, 실데이터 기준)

| 표 | 범위 | 결과 | 비고 |
|---|---|---|---|
| 피자 150g 기준 | 20메뉴 × 116행 × 5영양소 = **580셀** | ✅ 580/580 일치 | 100g값×1.5, 엣지 행은 (베이스 100g값×베이스중량 + 엣지 절대값)÷총중량 후 ×1.5 — 손계산과 ±1 이내 |
| 피자 조각 기준 | 116행, **696셀** | ✅ 696/696 일치 | 조각수 L8/R6(1인용 6, 고구마 피자는 사용자 오버라이드 L16/R12), 1조각 ≥100g→1/2/3조각 규칙, 1회중량·총조각중량·1회조각수 포함. 1인용 4건 정상 출력 |
| 사이드·파스타 | 13행 78셀 | ✅ 78/78 일치 | 1회분 저장값 그대로(오븐치즈떡볶이·스파게티 2종·치킨바사삭 2종·핫윙·찰치즈볼·새우링·닭다리살·콘코울슬로·츄러스·꿀·할라피뇨쨈) |
| 추가토핑 | 13행 78셀 | ✅ 78/78 일치 | 저장값(1회분) 그대로, 정수 반올림 |
| 음료 | 2행 12셀 | ❌→✅ | 수정 전 코카콜라 1.25L **6,875kcal/당류 1,719g**, 500ml 1,080kcal (§4-B2). 수정 후 550/138·216/54 |
| 세트박스·하프앤하프 | 4행 | ✅ | 열량(최소~최대)·중량만 계산 — **사용자 결정(2026-09-16): 열량만 표시하면 됨**. 하프앤하프 L 1776~3424kcal, R 1334~2282kcal는 크러스트·엣지 변형 중 최저 2개/최고 2개 평균으로 산출, 세트는 슬롯별 후보 min/max 합산 |
| 엑셀 통합 다운로드 | 6시트 | ✅ | 앱 exporter로 생성한 파일을 다시 읽어 시트 셀 = 화면 값 확인(빈값은 "—"로 통일 출력) |
| PDF 통합 출력 | 인쇄 HTML | ✅ | 같은 시트 객체를 pass-through, 재반올림 없음. 사이드 10·파스타 4개 제한은 현재 해당 없음 |
| 계산 결과 탭 ↔ 150g 표 | 고르곤졸라 석쇠L | ✅ | 313.71 → 471(×1.5), 320으로 바꾸면 480 |

## §2. 연동 정합성

| 항목 | 결과 | 비고 |
|---|---|---|
| 영양 메뉴 ↔ 메뉴마스터 코드 | ❌→✅ | 영양 메뉴는 베이스 코드(`P-OR-006`), 마스터는 full 코드(`P-OR-006-L`) — 라벨 컨텍스트 조회가 실패하던 것을 수정(§4-B4). 실데이터 **35/35** 조회 성공, 마스터에 없는 영양 메뉴 0 |
| 영양 메뉴명 ↔ 마스터명 | ❌→✅ | 재확인 결과 "…피자 L" 불일치는 진단 스크립트의 맵 순서 버그(L/R 중 먼저 만난 쪽을 비교 대상으로 착각)였고 실제로는 정상(영양 메뉴명은 항상 L 마스터명과 일치). 진짜 불일치는 1인용 4건(`P-ONE-001~004`, 마스터 "…피자(1인용)" vs 영양 메뉴 "…(1인용)" — "피자" 누락)뿐이었고 2026-09-16 백필로 정정. 재발 방지로 메뉴마스터 저장 시 이름/카테고리만 바뀌어도(코드 불변) 연결 store를 자동 새로고침하도록 구현(§5-2, `collectIdentitySyncRows`) |
| 단종 메뉴가 표에 포함되는지 | ✅ | 실데이터 35건 중 전 사이즈 단종 없음. 사용자 결정에 따라 단종 자동 제외 구현(§5-3) |
| 크러스트 완결성 | ✅ | 피자 20건 전부 석쇠L/석쇠R/씬바사삭L(1인용은 전용 슬롯) 입력 완료, 중량 0·포화지방 미입력 없음 (1차 감사의 "샘스테이크 당류·포화지방 미입력"은 낡은 서버 사본의 문제였음) |
| 알레르기 21종 목록 | ⚠️→✅ | 2026-09-16 결정: 법정 항목이 아닌 **아몬드** 제외, `ALLERGEN_SEED` 21종(굴·전복·홍합 개별 표시)만 유지. 화면 문구도 "법정 21종"으로 정정(§5-4) |
| 알레르기 집계 사슬 | ✅ | 실데이터에서 **경고 배너 0건**: 도우/엣지 구성 5건 존재, 레시피 구성품 전부 식자재 마스터와 매칭, 비음료 메뉴 전부 레시피 있음. 표 예: 페페로니 "밀, 대두, 우유, 토마토, 돼지고기, 닭고기, 쇠고기", 고르곤졸라 "밀, 대두, 우유". 알레르기가 빈 메뉴는 할라피뇨쨈·꿀·콜라 2종뿐(해당 없음으로 타당). 1차 감사의 "도우 미설정·18종 미등록·1인용 레시피 없음"은 전부 낡은 서버 사본의 문제였고, 조용한 누락을 막는 배너(§4-B5)는 예방 장치로 유지 |
| 원산지 표시판 | ✅ | `cost_ingredients.origin`·레시피 기반 정상 생성. 코드 키 단일 대소문자 문제(S6)는 현재 코드가 전부 대문자라 실영향 없음 |
| 중복 데이터 | ⚠️→✅ | 1차 감사 당시 우려였으나 2026-09-16 재확인 결과 `nutrition_raw_values` 67건 전부 유니크(중복 그룹 0건) — 해당 없음 |

## §3. 화면·출력물 실동작

| 화면 | 확인 항목 | 결과 |
|---|---|---|
| `/nutrition/menu` 베이스 | 고르곤졸라 석쇠L 열량 313.71→320 저장 → 계산 결과 탭 320 → `/nutrition/export` 150g 480 → 313.71로 되돌려 저장 → 서버 313.71 확인 | ✅ 왕복 |
| `/nutrition/menu` 5탭 | 베이스/엣지/추가토핑/계산 결과/세트 계산 렌더, 중복 감지 배너, 추가토핑 중량 비례 계산(앞선 커밋) | ✅ |
| `/nutrition/export` 영양성분표 | 6탭 렌더, 150g/조각 토글, 조각수 설정 모달, 알레르기 확인 배너(신규), 엑셀/PDF | ✅ |
| `/nutrition/allergen`, `/nutrition/origin`, `/nutrition` | 렌더·콘솔 에러 0 | ✅ |
| 엣지 저장 왕복, 세트 계산 저장/삭제, 엑셀 가져오기 저장, 중복 정리, 고아 삭제 | ⏭ | 실데이터 변경 최소화 — 가져오기·정리는 확인 단계까지만 |

## §4. 발견 버그 (수정 완료, 커밋 `b17025a9`, `35f41200`)

| # | 심각도 | 내용 | 근거 | 수정 |
|---|---|---|---|---|
| B1 | **HIGH** | 표의 "포화지방" 열이 포화지방 미입력이면 **조지방(총지방)을 대신 출력** — 다른 영양소를 포화지방으로 표시 | `lib/nutrition/label/_utils.js labelFieldValue` (기존 테스트가 이 동작을 명시하고 있었음) | satFat만 사용, 미입력은 "—". 실데이터엔 현재 해당 셀 없음(샘스테이크는 둘 다 미입력) |
| B2 | **HIGH** | 음료가 1병 전체 값을 100ml 기준으로 보고 용량으로 재곱셈 — 코카콜라 1.25L **6,875kcal/당류 1,719g**, 500ml 1,080kcal이 화면·포스터·엑셀에 출력 | `sheets/beverage.js`; basis 플래그 도입 전 저장된 행 2건 | 음료는 항상 저장값 그대로(입력 화면 규칙과 일치). 550/138/38·216/54/15로 정정 |
| B3 | **HIGH** | 조각 기준 표의 1인용 피자가 전용 슬롯이 아닌 씬바사삭L만 읽어 전부 "—" | `sheets/pizza.js:201`, 고르곤졸라·페페로니(1인용) | 150g 표와 같은 규칙(전용 슬롯 우선). 6조각/186g/681kcal 등 출력 |
| B4 | MEDIUM | 라벨 컨텍스트 `masterByCode`가 full 코드만 키로 잡아 베이스 코드 영양 메뉴 조회 실패 → 카테고리·1인용 판정이 옛 복사본 의존 | `label/context.js`, `menu-group.js` | `buildMasterByCode`(full+베이스 키), 베이스 메뉴 목록도 동일 헬퍼 |
| B5 | MEDIUM | 알레르기 집계 사슬이 끊겨도 아무 표시 없이 빈칸/불완전 목록 출력 | `allergen/aggregate.js` | `buildAllergenCoverageWarnings` + 표 출력 화면 배너(no-print): 도우 미설정·레시피 없음·마스터에 없는 구성품·알레르기 빈 식자재 목록 |
| B6 | LOW | 150g 표 weight 열이 기준량(150)인데 머리글 "총중량" | 앞선 커밋 `74e32024` | "기준중량"으로 통일 |

### 데이터 항목 (서버 정정 후 재확인)
| # | 1차 감사 판정 | 실데이터 재확인 결과 |
|---|---|---|
| D1 | 도우/엣지 구성 비어 있음 → 밀·대두 누락 | **해당 없음** — 사용자 PC엔 5건 존재, 표에 밀·대두 정상 반영 |
| D2 | 레시피 구성품 18종 미등록 | **해당 없음** — 151개 식자재 전부 등록·알레르기 입력됨(서버 사본에만 없었음) |
| D3 | 영양 메뉴 8건뿐 | **해당 없음** — 35건(피자 17·1인용 4·사이드 13·음료 2) |
| D4 | 샘스테이크 포화지방 미입력, 1인용 레시피 없음 | **해당 없음** — 전 크러스트 입력 완료, 레시피 있음 |
| D5 | 영양 메뉴명 4건이 마스터명과 다름("…피자 L"이 L/R 공용 행 이름) | ✅ 해결 — 실제로는 1인용 4건("피자" 누락)만 진짜 불일치였고(§2), 2026-09-16 백필로 정정 + 재발 방지 자동 전파 구현(§5-2) |
| **D6** | (신규) **운영 PC 브라우저 → 서버 동기화 불능** | ✅ 원인 확정·수정. 운영 PC는 localhost 접속(쓰기 모드 정상)이었고, 문제는 `lib/db/server-sync.js`의 재시도 방식이었다: 서버가 4xx로 영구 거절한 배치(예: catalog 미등록 store·잘못된 행 1건)를 통째로 큐 **앞**에 되돌려 30초마다 같은 실패를 반복 → 그 뒤에 쌓인 모든 저장이 영원히 막힘(독약 알약). 수정: 4xx 배치는 작업 단위로 재전송해 거절된 것만 격리(`getServerStoreSyncDeadLetters`, `/settings/sync` 경고 카드로 노출)하고 나머지는 계속 전송. 5xx/네트워크는 종전대로 되돌려 재시도. 회귀 테스트 `__tests__/lib/server-sync-queue-dead-letter.test.mjs`. 남은 한계: 큐가 메모리 보관이라 30초 재시도 대기 중 탭을 닫으면 beacon 전송에 실패한 잔여분은 유실될 수 있음 |

## §5. 정책 판단 필요 (수정하지 않음)
1. ~~세트박스·하프앤하프 5종 확장~~ → **결정됨(2026-09-16): 열량만 표시. 현행 유지.**
2. ~~메뉴마스터에서 이름/카테고리만 바꾸면 영양 메뉴에 전파되지 않음~~ → **결정됨(2026-09-16): 자동 전파.**
   구현: `upsertMenuMaster`가 menuCode는 그대로 두고 menuName/category만 바뀐 저장을 감지하면
   `collectIdentitySyncRows`로 연결 store(영양 메뉴·판매가·메뉴 레시피 — 이력(`menu_recipe_versions`)
   제외)의 같은 코드 행을 새 이름/카테고리로 제자리 새로고침한다(코드 변경 캐스케이드와 같은 트랜잭션
   경로 재사용). L/R처럼 base 코드를 공유하는 형제 규격 중 어느 쪽을 고쳐도 그 base의 영양 메뉴 1건이
   갱신된다. 저장 토스트에 "메뉴명 동기화: N건" 표시. 회귀 테스트
   `__tests__/lib/menu-master-identity-sync.test.mjs`(5건), 브라우저에서 실데이터로 왕복 검증 완료.
   기존 데이터 중 1인용 4건("…(1인용)"에 "피자" 누락)은 1회 백필로 정정.
3. ~~단종 메뉴 자동 제외 여부~~ → **결정됨(2026-09-16): 메뉴마스터에서 단종되면 자동 제외.** 구현: 영양성분표는 베이스 코드에 묶인 L/R이 전부 단종일 때 제외(`/nutrition/menu`와 동일 규칙), 원산지 표시판·알레르기 매트릭스는 `extractExcludedMenuSets`가 단종 행 코드를 제외 목록에 합산(이름 기준 제외는 같은 이름의 행이 전부 단종일 때만 — 판매 중인 사이즈 보호). 숨김(hidden)은 대상 아님.
4. ~~알레르기 목록의 "아몬드"(국내 법정 항목 아님) 유지 여부와 "법정 22종" 문구~~ → **결정됨(2026-09-16): 아몬드 제외, 법정 21종으로 정정.**

## §6. 미실행 항목
- 파괴적 액션(전체삭제·중복 정리·고아 삭제·가져오기 저장)은 확인 단계까지만.
- (세트박스 5종 확장은 "열량만" 결정으로 종결.)

## §7. 서버 상태
- 1차 감사 중 서버에 남긴 변경: 없음(왕복 저장은 원값 313.71로 복원, `updatedAt`만 갱신). 재계산 스크립트는 읽기 전용.
- 정정 단계: 서버를 사용자 백업(2026-09-16 15:16 내보내기)으로 통째로 교체(§0). 교체 전 pg_dump 스냅샷 보관.
  교체 후 서버 = 사용자 PC(45개 공개 store 행 수 전부 일치, 분기행 0, QA 잔재 0).
